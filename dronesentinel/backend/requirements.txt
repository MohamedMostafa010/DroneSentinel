# ─────────────────────────────────────────────────────────────
# DroneSentinel — Backend requirements  (Python 3.11+)
# Pinned to the versions the project actually runs on.
# ─────────────────────────────────────────────────────────────

# ── Web / API ────────────────────────────────────────────────
fastapi==0.135.1
uvicorn[standard]==0.41.0          # ASGI server ([standard] needed for --reload)
pydantic==2.12.5
slowapi==0.1.9                     # request rate limiting

# ── Detection & Tracking (AI) ────────────────────────────────
ultralytics==8.4.21                # YOLOv26s inference / training
numpy==2.1.3
scipy==1.17.1                      # Hungarian algorithm for the BoT-SORT tracker
opencv-python>=4.8.0               # cv2  (see OpenCV note below)
nvidia-ml-py==13.590.48            # pynvml — live GPU stats
PyYAML==6.0.3                      # dataset / config YAML

# PyTorch with CUDA 12.8 (RTX 4050) — from PyTorch's own index
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128

# ── Security ─────────────────────────────────────────────────
pycryptodome==3.23.0               # AES-256 encryption at rest
python-jose[cryptography]==3.5.0   # JWT access / refresh tokens

# ── Data & Reporting ─────────────────────────────────────────
pandas==2.3.3                      # CSV / JSON session export
reportlab==4.4.10                  # PDF report generation

# ─────────────────────────────────────────────────────────────
# NOTE — OpenCV & GPU video decode:
#   The live GPU path uses cv2.cudacodec (NVDEC), which exists ONLY in a
#   CUDA-enabled OpenCV build compiled from source (the dev machine ran a
#   custom OpenCV 4.13.0 — not available on PyPI). The `opencv-python` wheel
#   above is standard OpenCV; the code automatically falls back to CPU FFmpeg
#   decode when cudacodec is absent, so detection still works — you just lose
#   GPU-accelerated RTSP decoding.
# ─────────────────────────────────────────────────────────────
